import re
#S=[pass wer007]
def solution(S):
    r=[];
    a=S.split(" ");
    p = re.compile('[a-zA-Z]+[0-9]+')
    for x in a:
        m=p.match(x)
        if(m!=None):
            r.append(x);
    r.sort();
    return len(r[-1]);
